{
  "rules": {
    "sessions": {
      "$code": {
        ".read": "true",
        ".write": "true",
        ".validate": "newData.hasChildren(['settings'])"
      }
    },
    ".read": false,
    ".write": false
  }
}
